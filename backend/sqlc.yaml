version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries"
    schema: "../utils/database/migrations"
    gen:
      go:
        package: "sqlgen"
        out: "internal/sqlgen"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true
